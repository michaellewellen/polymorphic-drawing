namespace Poly;
// Represents a graphical element that can be printed to the console.
public interface IGraphic2D
{
    // draws the grapic on the screen; returns true if successful
    bool Display();
}
